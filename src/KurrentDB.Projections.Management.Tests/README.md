# KurrentDB.Projections.Management.Tests

NUnit tests for the projections management layer: lifecycle, gRPC/HTTP APIs, subsystem coordination, and integration scenarios.

## Jint tests

The `Services/Jint/` tests are here temporarily because they are NUnit and `KurrentDB.Projections.JavaScript.Tests` is xUnit. They should move to the JavaScript test project once converted.
