@* Copyright (c) Kurrent, Inc and/or licensed to Kurrent, Inc under one or more agreements. *@
@* Kurrent, Inc licenses this file to you under the Kurrent License v1 (see LICENSE.md). *@

@page "/ui/logs"
@attribute [Authorize(Policy = UiPolicies.ViewLogs)]
@inherits KurrentDB.Components.Licensed.LicensedPage
@using KurrentDB.Components.Shared
@using Microsoft.AspNetCore.Authorization

@* Gated on Node.Information.Log via the policy; a denied user never reaches the page, so LogsView
   (and its log-sink subscription) is never instantiated. *@
<LogsView/>

@code {
	protected override string PageName => "Real-time logs";
}
